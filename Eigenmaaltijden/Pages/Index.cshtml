@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
    @if (Model.isLoggedIn == true)
    {
        <h1 class="display-4">Welkom @Model.getName()</h1>
    }
    else
    {
        <h1 class="display-4">Welkom op EigenMaaltijd</h1>
    }
</div>
